{
	"scripts": {
		"start": "nodemon",
		"build": "rm -rf dist && mkdir -p dist && yarn tsc && ./makeBundle.js"
	},
	"engines": {
		"node": ">=21.0.0"
	  },
	"devDependencies": {
		"@types/node": "^22.8.4",
		"esmify": "^2.1.1",
		"nodemon": "^3.1.7",
		"recursive-readdir": "^2.2.3"
	},
	"dependencies": {
		"browserify": "^17.0.1",
		"crypto-bot-api": "^0.3.3",
		"fs-extra": "^11.2.0",
		"mongodb": "^6.10.0",
		"socket.io": "^4.8.1",
		"typescript": "^5.6.3"
	}
}
